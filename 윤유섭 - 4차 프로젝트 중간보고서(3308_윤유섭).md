# 4차 프로젝트 중간보고서

**학번/이름**: 3308 윤유섭  
**보고서 작성일**: 2026년 6월 1일  
**프로젝트명**: 뱅크라면 (Bankramen) Android 앱  
**대상 기간**: 2026년 5월 20일 ~ 2026년 5월 30일 (3차 보고서 이후)

---

## 1. 3차 보고서 이후 추가/변경 사항 요약

3차 보고서(2026년 5월 20일) 기준으로 이후 3개의 커밋이 진행되었습니다.

| 날짜 | 커밋 | 내용 |
|------|------|------|
| 2026-05-27 | `05e5319` | 카카오 서버 교환 실패 시 디버그 빌드 우회 세션 추가 |
| 2026-05-27 | `c1f8487` | 라이브 API 명세와 Android 클라이언트 전면 동기화 |
| 2026-05-30 | `e6b6962` | 라이브 API의 시간 포맷 대응을 위한 Gson 어댑터 추가 |

---

## 2. 세부 변경 내역

### 2-1. 카카오 로그인 디버그 우회 처리 (05e5319, 2026-05-27)

**배경 및 문제**  
카카오 서버 측 인가 코드 교환(`/auth/kakao/callback`) 흐름이 실패하고 있어, 로그인 이후 화면 개발 및 검증이 불가능한 상태였습니다.

**해결 방법**  
`KakaoLoginResult.DirectToken` 케이스를 추가하여, 서버에서 직접 액세스 토큰을 리디렉션 URL 파라미터로 넘겨줄 경우 별도 서버 교환 없이 로컬 세션을 생성하도록 처리했습니다.

**변경된 파일**
- `core/auth/KakaoLoginResult.kt` — `DirectToken` sealed class 추가
- `core/auth/KakaoLoginStateStore.kt` — `completeTokenSuccess()` 메서드 및 `ResultTypeDirectToken` 처리 추가
- `core/auth/KakaoLoginCallbackHandler.kt` — 리디렉션 URL에서 `accessToken` 파라미터를 먼저 탐지하는 로직 추가
- `core/auth/AuthRepository.kt` — `completePendingKakaoLogin()`에 `DirectToken` 분기 처리 추가

**설계 원칙**
- Release 빌드에는 영향 없음 (디버그 우회 목적)
- 서버 측 토큰 발급이 안정화되면 `DirectToken` 경로는 제거 예정

---

### 2-2. 라이브 API 명세 전면 동기화 (c1f8487, 2026-05-27)

**배경 및 문제**  
기존에 체크인된 `openapi/bankramen-api.json`이 실제 라이브 서버의 Swagger 명세와 달라서, 자동 생성(`generateBankramenApi`)된 클라이언트 코드가 실제 엔드포인트와 맞지 않는 문제가 있었습니다.

**변경 내용**

#### OpenAPI 명세 갱신 (`openapi/bankramen-api.json`)
- 기존 명세(~720라인)를 실제 라이브 명세로 전면 교체
- 새로 반영된 API 태그:
  - `디바이스토큰 API` — FCM 디바이스 토큰 등록/관리
  - `정기결제 API` — 정기결제 등록, 조회, 삭제, 확정
- 18개 엔드포인트로 정리됨:

```
GET    /auth/kakao/login
POST   /auth/kakao/token            (디바이스 토큰 등록)
POST   /auth/kakao/reissue
POST   /auth/kakao/logout
GET    /auth/kakao/callback
POST   /transactions
POST   /transactions/payment-notifications
GET    /transactions/recent
GET    /transactions/expenses
GET    /transactions/incomes
PATCH  /transactions/{id}/category
DELETE /transactions/{id}
POST   /recurring-payments
GET    /recurring-payments
PATCH  /recurring-payments/{id}/confirm
DELETE /recurring-payments/{id}
GET    /reports/monthly/summary
GET    /reports/monthly/categories
GET    /categories
```

#### 신규 파일: `DeviceTokenRepository.kt`
FCM 푸시 토큰을 서버에 등록하는 Repository입니다.
- `APIApi.saveDeviceToken(memberId, deviceTokenRequest)` 호출
- 세션에서 `userId`를 조회한 후 UUID 변환하여 전송
- 토큰이 없거나 인증 세션이 없으면 조용히 스킵

```kotlin
class DeviceTokenRepository(context: Context, ...) {
    suspend fun registerDeviceToken(token: String): Result<Unit>
}
```

#### 신규 파일: `RecurringPaymentRepository.kt`
정기결제 전체 CRUD를 담당하는 Repository입니다.

| 메서드 | API | 설명 |
|--------|-----|------|
| `getRecurringPayments()` | `GET /recurring-payments` | 목록 조회 |
| `createRecurringPayment(...)` | `POST /recurring-payments` | 등록 |
| `confirmRecurringPayment(...)` | `PATCH /recurring-payments/{id}/confirm` | 자동 감지 후보 확정 |
| `deleteRecurringPayment(...)` | `DELETE /recurring-payments/{id}` | 삭제 |

내부 도메인 모델 (`RecurringPaymentSnapshot`, `RecurringPaymentEntry`)로 변환하여 반환합니다.

#### 변경: `AppRoot.kt` / `BankramenApplication.kt`
- 로그인 완료 이후 FCM 토큰을 즉시 서버에 등록하는 흐름 추가
- `RootViewModel.completeLogin()` → `registerCurrentDeviceToken()` 호출
- `PostLoginOnboardingScreen` 분기 처리

#### 신규: `BankramenApiCoverageTest.kt` (287라인)
생성된 Retrofit 클라이언트가 올바른 HTTP 메서드/경로로 요청을 생성하는지 검증하는 단위 테스트입니다.
- 실제 네트워크 없이 `RecordingInterceptor`로 요청 URL을 캡처
- 모든 API 엔드포인트(login, reissue, logout, device-token, recurring-payments, categories, reports, transactions 등)를 호출하고 예상 HTTP 요청과 대조

---

### 2-3. 라이브 API 시간 포맷 대응 Gson 어댑터 (e6b6962, 2026-05-30)

**배경 및 문제**  
라이브 서버의 거래 응답에서 `transactionTime` 필드가 `{ hour, minute, second, nano }` 객체 형태가 아닌 `"HH:mm:ss"` 문자열 형태로 내려옵니다. 생성된 `BankramenLocalTime` 모델은 객체 형태를 기대하고 있어 파싱이 실패하는 문제가 있었습니다.

**해결 방법**  
`BankramenGson.kt`에 커스텀 `JsonDeserializer<BankramenLocalTime>`를 등록하여 두 가지 포맷을 모두 수용합니다.

```kotlin
object BankramenGson {
    val gsonBuilder: GsonBuilder by lazy {
        Serializer.gsonBuilder
            .registerTypeAdapter(BankramenLocalTime::class.java, BankramenLocalTimeJsonAdapter())
    }
}
```

- 문자열(`"14:30:00"`) → `LocalTime.parse()` → `BankramenLocalTime` 변환
- 객체 형태 → 기존 필드 매핑 그대로 유지
- `null` / `JsonNull` → `null` 반환

**설계 원칙**
- 생성된 모델 파일(`BankramenLocalTime.kt`)은 직접 수정하지 않음 → 재생성 시 덮어씌워지는 문제 방지
- OpenAPI 명세와 실제 서버 응답이 일치하게 되면 이 어댑터는 제거 예정

**변경된 파일**
- `core/api/BankramenGson.kt` — 신규 생성, `BankramenLocalTimeJsonAdapter` 구현
- `core/api/BankramenRetrofitBuilder.kt` — `BankramenGson.gsonBuilder`를 Retrofit에 등록
- `core/api/BankramenApiCoverageTest.kt` — 시간 포맷 관련 커버리지 테스트 업데이트

---

## 3. 현재 전체 구조 요약

```
app/src/main/java/com/uson/myapplication/
├── AppRoot.kt                          # 인증 상태 기반 화면 전환 (RootViewModel 포함)
├── BankramenApplication.kt             # Application 클래스, 알림 채널 설정
├── MainActivity.kt                     # 단일 Activity, 카카오 콜백 처리
│
├── core/
│   ├── api/
│   │   ├── BankramenApiFactory.kt      # Retrofit API 인스턴스 팩토리
│   │   ├── BankramenApiInterceptor.kt  # Authorization 헤더 주입
│   │   ├── BankramenGson.kt            # [신규] BankramenLocalTime 커스텀 Gson 어댑터
│   │   ├── BankramenGsonBuilder.kt
│   │   └── BankramenRetrofitBuilder.kt
│   │
│   ├── auth/
│   │   ├── AuthRepository.kt           # 카카오 로그인 흐름 통합 (DirectToken 분기 추가)
│   │   ├── AuthGraph.kt                # 의존성 공급 객체
│   │   ├── AuthSessionStore.kt         # SharedPreferences 기반 세션 저장
│   │   ├── AuthSessionManager.kt       # 세션 bootstrap / 만료 처리
│   │   ├── GeneratedAuthGateway.kt     # generated API 기반 인증 Gateway
│   │   ├── KakaoLoginResult.kt         # [변경] DirectToken sealed class 추가
│   │   ├── KakaoLoginStateStore.kt     # [변경] DirectToken 저장/소비 처리
│   │   ├── KakaoLoginCallbackHandler.kt# [변경] accessToken 우선 탐지
│   │   ├── KakaoRestLoginLauncher.kt   # WebView 기반 카카오 로그인 시작
│   │   ├── KakaoWebViewActivity.kt     # 카카오 WebView
│   │   └── DeviceTokenRepository.kt   # [신규] FCM 디바이스 토큰 서버 등록
│   │
│   ├── notification/
│   │   ├── TransactionNotificationParser.kt  # 금융 앱 알림 파싱
│   │   ├── ParsedTransactionNotification.kt  # 파싱 결과 도메인 모델
│   │   ├── NotificationStore.kt              # Flow 기반 인메모리 저장소
│   │   └── NotificationDebugRepository.kt    # 디버그 목적 알림 조회
│   │
│   ├── local/
│   │   ├── BankramenDatabase.kt        # Room 데이터베이스
│   │   ├── ParsedNotificationDao.kt    # 알림 DAO
│   │   └── ParsedNotificationEntity.kt # 알림 Entity
│   │
│   ├── recurring/
│   │   └── RecurringPaymentRepository.kt  # [신규] 정기결제 CRUD Repository
│   │
│   └── transaction/
│       └── NotificationTransactionUploader.kt  # 파싱된 알림 → 서버 업로드
│
├── feature/
│   ├── home/
│   │   ├── HomeScreen.kt           # 홈 화면 Compose UI
│   │   ├── HomeViewModel.kt        # 홈 ViewModel (정기결제 연동)
│   │   └── HomeApiRepository.kt    # 거래/리포트 API 호출
│   │
│   └── login/
│       ├── LoginRoute.kt           # 로그인 Route (콜백 처리 포함)
│       ├── LoginScreen.kt          # 로그인 Compose UI
│       ├── LoginUiState.kt         # 로그인 UI 상태
│       └── LoginViewModel.kt       # 로그인 ViewModel
│
└── service/
    ├── TransactionNotificationService.kt       # NotificationListenerService
    └── BankramenFirebaseMessagingService.kt     # FCM 서비스
```

---

## 4. API 연동 현황

| API 그룹 | 엔드포인트 수 | 연동 상태 |
|---------|-------------|---------|
| 인증 API (카카오 OAuth) | 4개 | 완료 |
| 디바이스 토큰 API | 1개 | **신규 완료** |
| 거래 내역 API | 6개 | 완료 |
| 월별 리포트 API | 2개 | 완료 |
| 정기결제 API | 4개 | **신규 완료** |
| 카테고리 API | 1개 | 완료 |
| **합계** | **18개** | **전체 완료** |

---

## 5. 테스트 현황

| 테스트 파일 | 검증 내용 |
|-----------|---------|
| `BankramenApiCoverageTest` | 18개 API 엔드포인트가 올바른 HTTP 메서드/경로로 요청되는지 검증 |
| `TransactionNotificationParserTest` | 금융 앱 알림 텍스트 파싱, 자기 이체 필터링, 금액 추출 검증 |
| `HomeApiRepositoryTest` | `BankramenLocalTime` 문자열/객체 포맷 파싱 검증 포함 |

---

## 6. 향후 계획

1. **서버 토큰 발급 안정화 후** `DirectToken` 우회 경로 제거
2. **OpenAPI 명세 실제 서버와 완전 일치 후** `BankramenLocalTimeJsonAdapter` 제거
3. 정기결제 UI 화면(정기결제 목록 / 등록 화면) Compose 구현 완성
4. 홈 화면 정기결제 섹션 UI 마무리